# compile.config.civet - default compiler configuration

import {
	LOG, LOGVALUE, DBG, DBGVALUE,
	} from '../src/lib/logger.lib.ts'
import {withExt} from '../src/lib/fs.lib.ts'

export default new Object {
	hCompilers: {
		'.civet': {
			getOutPaths: (path: string) =>
				return ['abc']
			tester: () => true
			compiler: (hInfo: any) => 'compiled'
			}
		}

	hPostProcessors: {}
	}
