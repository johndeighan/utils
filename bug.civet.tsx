"use strict";
function useNode(x: unknown): boolean { () => {

		return (
			isObject(x)(  this.length(!undef, null, regex, array
				&& (
						!this.className
					|| (x?.constructor?.name === this.className)
					)))
			)
} }
