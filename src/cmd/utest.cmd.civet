# utest.cmd.civet

import {undef} from 'datatypes'
import {
	stdChecks, allNonOptions, tryCmd, doUnitTest,
	} from 'base-utils'
import {LOG, DBG} from 'logger'

stdChecks()

# ---------------------------------------------------------------------------

for stub of allNonOptions()
	purpose := (stub == 'temp') ? undef : 'lib'
	await tryCmd doUnitTest, stub, purpose
LOG "DONE"