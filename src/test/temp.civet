import {LOG} from 'logger'
import {analyze} from 'typescript'
import {a, A, civet2ts} from 'civet'

# ---------------------------------------------------------------------------

code := """
	func := (s: string): string =>
		return s + t + '.txt'
	func('abc')
	"""
tsCode := civet2ts(code)
analysis := analyze(civet2ts(code))
block := analysis.asString()
LOG block