# temp2.civet

import {undef, defined, notdefined} from 'datatypes'
import {LOG, DBG} from 'logger'
import {findSrcFile} from 'fsys'
import {compileCivetFiles} from 'civet'
import {execCmds} from 'exec'

# ---------------------------------------------------------------------------

LOG "Hello"
lResults := await compileCivetFiles(['datatypes','llutils'])
console.dir lResults
