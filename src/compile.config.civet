# compile.config.civet

import {convertCieloFile, convertCivetFile} from './src/lib/llutils.js'

# ---------------------------------------------------------------------------

export hConfig = {
	'.cielo': (path, hOptions) => convertCieloFile(path, hOptions)
	'.civet': (path, hOptions) => convertCivetFile(path, hOptions)
	}

